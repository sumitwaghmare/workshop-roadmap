{
  "name": "deploy-tool",
  "version": "1.0.0",
  "description": "Lightweight deployment tool – replaces robocopy batch scripts with a web UI",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.0"
  }
}
